import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#242A32",
  },
  header: {
    width: "100%",
    marginTop: 30,
  },

  text1: {
    left: 50,
    fontSize: 25,
    alignItems: "center",
    color: "#ECECEC",
  },
});
